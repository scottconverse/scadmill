# ScadMill

ScadMill is a source-first OpenSCAD workbench for desktop and web. It combines an OpenSCAD code editor with a live, interactive model viewer while keeping the unmodified OpenSCAD engine out of process.

The project is being delivered milestone by milestone from the clean-room functional specification in [`spec/scadmill-spec-v0.6.md`](spec/scadmill-spec-v0.6.md). M0 and M1 are accepted, and the exact M2 candidate passed its local, clean Windows Sandbox, hosted CI, and isolated owner similarity gates. Every unblocked M3 implementation and lifecycle gate is green; Q-0033 and Q-0034 still block formal M3 close while unrelated M4 work proceeds. Open decisions remain isolated in [`spec/QUESTIONS.md`](spec/QUESTIONS.md) without retroactively invalidating accepted milestones.

Start with the [user guide](docs/USER-GUIDE.md) for product workflows, the [FAQ](docs/FAQ.md) for current limitations, and [ARCHITECTURE.md](ARCHITECTURE.md) for the runtime and trust boundaries.

## Current capabilities

- Edit OpenSCAD source in a CodeMirror-based workbench and render it through the out-of-process native engine adapter.
- Highlight OpenSCAD keywords, built-ins, literals, special variables, comments, operators, and statement modifiers through a fresh version-labeled Lezer grammar.
- Offer context-aware completion for the version-labeled provisional built-in corpus, lexically visible current-file symbols, and declarations pulled recursively from project files referenced by `include`/`use`. Uncached structural project indexing runs in a dedicated worker, while the workerless fallback yields cooperatively; both retain lazy source lookup, bounded exact-source caching, declaring-file-relative path resolution contained inside the project, textual replay, OpenSCAD's nested-`use` visibility, and support for individual project files up to 2.1 million code units within an 8-million-code-unit traversal budget. Signatures identify their project-relative source; the deterministic `cube` skeleton remains provisional while Q-0013 is open.
- Work across reorderable, keyboard-accessible document tabs with isolated edit/undo sessions, dirty-state announcements, project-backed save, clean close/reopen commands, and render results bound to the exact source snapshot. Dirty-close and final-tab policy remain explicitly parked in `spec/QUESTIONS.md`.
- Apply typed native-engine parameter overrides without rewriting source, including validated numbers, booleans, strings, and numeric vectors. Preview and full renders use separate timeouts and quality policies; F5 requests preview and F6 requests full geometry.
- Stage complete multi-file projects with byte-preserving binary assets, resolve `include`/`use` relative to each declaring source while rejecting paths outside the project, select 3D STL or 2D SVG from engine output, and export every Appendix A native format at full quality. PNG currently uses the engine's default camera; explicit `CameraPose` requests are rejected rather than losing `up` while Q-0021 is open.
- Stream interleaved, timestamped native stdout/stderr into per-run console history with quality, duration, exit state, geometry statistics, severity filtering, search, copy-all, clear, and a global 10,000-line cap. Parsed diagnostics are clickable, appear as themed editor squiggles and gutter markers, and can open an unloaded project file through the C6 source port.
- Debounce automatic renders at the configured default quality, cancel superseded or timed-out process trees, retain cancelled runs in console history, and keep the UI responsive while native work runs off the UI thread.
- Compare each accepted render with the previous successful render of the same document through application-owned SHA-256 geometry identity. The status bar reports **Geometry unchanged** for cosmetic edits or exposes signed volume, bounds, and triangle deltas for changed geometry; stale, failed, superseded, cross-dimension, missing-statistic, and identity-unavailable states remain explicit instead of inventing measurements.
- Inspect real STL geometry in a demand-driven Three.js viewer with orbit/pan/zoom, axis views, fit, perspective/orthographic projection, themed scene furniture, large-mesh degradation, point-to-point measurement, durable per-project/file pinned annotations, exact bounds, last-good error presentation, and PNG capture. Failed annotation metadata loads and saves stay visibly marked, preserve the current in-memory notes, and offer retry plus exact version-1 JSON export. STL decoding runs off the UI thread in the browser.
- Switch automatically between the 3D viewer and an allowlist-sanitized engine-produced SVG pane with exact model-space dimensions, cursor-centered pan/zoom, fit, and scale readout. An incompatible pinned mode shows an empty pane and notice while Q-0024 remains open.
- Extract stock OpenSCAD Customizer parameters structurally into grouped controls, apply non-destructive render and export overrides, write explicit values back into the original assignments, and exchange named sets in the stock JSON format.
- Choose folder-backed desktop projects with a native dialog or create/open named IndexedDB-backed browser workspaces without handling internal IDs. Edit text and preserve binary assets, create/rename/move files, use OS trash and reveal actions on desktop only, reconcile external edits per hunk in side-by-side or inline views, recover unsaved buffers, and retain reopenable recent projects. The browser omits actions whose OS semantics it cannot provide.
- Export full-quality 3MF, STL, OFF, AMF, SVG, DXF, or PNG artifacts with exact mesh summaries; web projects also import/export byte-preserving ZIP archives through a cancellable worker-backed path and create serverless single-file share links.
- Configure and persist all editor, rendering, engine, viewer, formatter, theme, AI, keybinding, and privacy settings. Desktop secrets use the OS keychain; browser secrets remain session-only unless the warning-labeled persistence option is enabled.
- Enforce the recorded OpenSCAD 2026.06.12 pin at runtime; an older PATH installation is left untouched and cannot enable rendering or export.
- Arrange the editor, viewer, parameters, diagnostics, and activity destinations in a resizable, collapsible workspace with keyboard commands, browser-profile persistence, provisional opaque per-project desktop persistence behind a storage-neutral port, and a single-column layout below 900 px or by default on mobile web. Native folder snapshots return their files, canonical project ID, and layout-identity material together; the validated canonical ID then drives later project operations.
- Follow the OS Light/Dark preference or switch among Light, Dark, High Contrast, and imported Appendix C custom themes without reloading the editor or viewer. Custom themes use the conservative opaque-sRGB and AA-contrast policy while Q-0005/Q-0006 remain open.
- Start from a suppressible welcome screen with safe blank-file and project actions, reopenable recent projects, and the three exact Appendix F sample models. Samples preserve the primary scratch identity, populate the Customizer immediately, enter the normal render pipeline, and require confirmation before replacing non-empty work.

## Accepted M1 boundary record

| Slice | Verified boundary | Still parked beyond accepted M1 |
|---|---|---|
| C0 layout | Default/narrow layouts, splitters, keyboard access, web-profile persistence, and console auto-open have automated coverage. M2 now adds a provisional restart-safe scratch and per-project desktop adapter keyed by an opaque digest of canonical identity material returned atomically with the native project snapshot. | Final project-workspace versus per-user-profile storage ownership remains owner-governed under Q-0008. The runtime port is storage-location-neutral so the provisional adapter can be replaced without changing layout state or project commands. |
| C1 editor | Tabs, dirty tracking, language support, completion infrastructure, diagnostics, editor settings, and C1-owned commands are implemented. | Save/retention blocks AC-1.d and AC-4.d under Q-0012; final command and corpus claims remain Q-0010/Q-0013. Q-0014 now governs only the historical M1 scheduling interpretation. |
| C4 native engine | Real 2D/3D geometry, typed parameters, multi-file staging, exports, streaming, timeout/cancel cleanup, debounce, and supersession are covered. | A true preview facet cap is Q-0003; explicit-camera PNG is Q-0021; native/WASM parity is the M3 gate. |
| C8 diagnostics | Ordered run history, raw output, structured diagnostics, filters, retention, inline markers, and navigation to current/open files are covered. | Loading a reported file not already open is Q-0020. |
| C12 theming | Complete shipped token sets, contrast checks, no component color literals, and live editor/viewer/console switching are covered. | Import-control ownership and final custom-color/contrast policy are Q-0004/Q-0005/Q-0006. |
| Quality gates | TypeScript, browser, native, desktop-shell, npm- and Rust-license, build, and provenance checks are green locally. | The owner similarity gate is CI-only and is never run locally. |

That table preserves the accepted M1 boundary as historical evidence. M2 now supplies the project-backed save, unloaded-file navigation, and read-only cross-file completion source map that were unavailable at M1, plus the visible custom-theme settings flow.

## M2 boundary record

| Slice | Delivered candidate | Conservatively parked |
|---|---|---|
| C2/C3 viewers | Native-backed STL and allowlist-sanitized SVG presentation, controlled camera, point-to-point measurement, per-project/file durable annotations with visible load/save failure recovery and exact JSON export, exact bounds, screenshots, 2D routing, and real-browser capture evidence. | Incompatible pinned-mode presentation, edge/face semantics, screenshot overlay composition, and keyboard geometry picking remain Q-0024/Q-0025/Q-0026/Q-0028. |
| C5 Customizer | Structural stock-annotation extraction, typed controls, non-destructive overrides, explicit source writes, named sets, and stock JSON round-trip evidence. | No C5 behavior is parked. |
| C6 files/projects/export | Blank scratch mode, functional File commands and shortcuts, discoverable browser Create/Open workspace actions, a typed native folder chooser with advanced manual-path fallback, desktop/web storage, safe file operations, per-hunk external-change reconciliation, recovery, recent projects, ZIP/share portability, and full-quality exports. M3 now presents the same durable recent-project list on the welcome screen. | Autosave-control semantics and application-owned export-destination picking remain Q-0030/Q-0031. Dirty/final-tab close policy remains Q-0018/Q-0019. |
| C9 settings | Strict all-section persistence/import/export, per-section restore, immediate application with compensating recovery that preserves concurrent edits and reports incomplete recovery, custom themes, OS-keychain desktop secrets, and warning-gated browser persistence. | Secret exclusion follows the stronger AC-9.c rule while Q-0027 remains open. |
| Automated gate evidence | Exact OpenSCAD 2026.06.12 enforcement, TypeScript/lint/build, unit/browser/native/desktop tests, npm- and Rust-license, generated-parser, and provenance gates. | The owner similarity gate remains isolated CI-only. |
| Packaged Windows evidence | From a clean HEAD, the host wrapper performs a frozen pnpm install/build and a clean locked Cargo release build, stages only the resulting canonical executable, and retains the source commit/tree, lockfile and executable hashes, exact build commands, tool versions, timestamps, and pre/post clean-worktree attestations. A network-disabled fresh Windows Sandbox then runs that executable through official `tauri-driver`, configures exact OpenSCAD 2026.06.12, renders and exports a measured cube, verifies normal restart and forced-process recovery, round-trips a synthetic secret through Windows Credential Manager, scans app and evidence files both while stored and after clear, and checks every exact guest process plus exact host Sandbox-session and staging cleanup. The lane also drives a real project's Files splitter from 260 to 300 through its production keyboard command, closes the exact app and WebView2 processes, launches fresh processes, reopens the same project, and requires the identical opaque storage key and exact serialized layout. | A packaged PASS is accepted only from retained evidence bound to the exact clean source commit and its self-built executable. Current run status belongs in the retained milestone-gate evidence rather than this static capability table. FR-2.5 qualification likewise comes only from retained external evidence bound to the exact candidate source tree and profiler harness. |

## M3 progress

| Slice | Current state | Remaining gate |
|---|---|---|
| C14 welcome | The first-run surface, durable suppress-on-launch preference, recent-project access, safe workspace replacement, and all three normative Appendix F samples are implemented. The exact-head real-engine fresh-profile journey passes in hosted Chromium on both Ubuntu and Windows, and a cached production-static reload restores and renders the exact sample path. | FR-15.13 thumbnails remain correctly scheduled for M4; no C14 behavior remains unverified at M3. |
| C4 web/WASM | The exact official source commit builds reproducibly inside the digest-pinned OpenSCAD WASM image, and the byte-verified JavaScript/WASM pair plus source-build manifest are locally staged at the ignored versioned static paths used by the worker. The web shell now selects the structured service through a real module worker, whose adapter owns the verified separately fetched runtime and atomic IndexedDB artifact cache. Loading progress is cache/network-neutral, failures preserve editing and offer a deduplicated retry, and a deterministic version mismatch is reported without a futile retry. The corrected hosted artifact embeds the required 2026.06.12 version and reproduced exactly. The exact-head real-engine AC-4.a lane proves exact F1/F2 binary-STL parity. F3 stops at raw byte 37, native `CR` versus WASM `LF`; the 75-byte length delta is consistent with one extra carriage return per native line, but fail-fast raw comparison does not establish normalized equality or rule out later differences. | Obtain the Q-0034 parity-rule amendment, complete the remaining raw comparisons under that rule, and close the Q-0033 public web-engine license gate. |
| C7 formatter | The parse-gated formatter produces all fifteen Appendix E goldens exactly and idempotently, preserves comments and Customizer annotations, formats documents and selections through the editor/menu command path, honors configurable indentation and format-on-save, and refuses malformed source without changing it. The four renderable golden pairs produce byte-identical STL output before and after formatting on OpenSCAD 2026.06.12. | C7 implementation and its local acceptance evidence are complete. |
| C13 platform/installers | `App` now receives one typed platform runtime. Separate web and desktop compositions provide engine, file, dialog, persistence, clipboard, keychain, menu, MCP, and window capability seams through discriminated availability records; browser-only globals are confined to the web adapter, and unavailable desktop-only features are absent from browser UI. The Tauri shell owns native File/Edit/View/Render/Help menus and a registered `.scad` editor association. Startup arguments, secondary Windows/Linux launches, and macOS open events feed one deduplicated FIFO queue; a clean workspace opens the exact selected entry in the focused existing window, while dirty work and recovery require the existing safe replacement flow. The exact-pinned official Tauri window-state plugin restores normal bounds and maximized/fullscreen state, and rejects an off-monitor saved position so the OS can place the window visibly. Exact-head hosted evidence signed and hash-verified the offline NSIS setup, matched the packaged and installed executable, proved the quoted `.scad` command and existing-instance routing, restored the exact normal rectangle, recovered a causally identified off-monitor size onto the visible desktop, uninstalled cleanly, and restored the prior association. The checksum-verified DMG and AppImage lanes also pass, with AppImage evidence bound to the exact child runtime and visible window. | C13 implementation and exact-candidate lifecycle evidence are complete. Owner-side macOS signing/notarization may lag under FR-13.4; the remaining M3 gates are Q-0033 and Q-0034. |

## M4 progress

| Slice | Current state | Remaining gate |
|---|---|---|
| FR-15.2 geometry delta | Each document retains its own previous accepted-success baseline across preview and full quality. Exact 2D SVG and 3D mesh bytes receive application-owned SHA-256 identities before presentation; malformed engine identities are not trusted, failed or superseded work cannot advance the baseline, and unavailable hashing is disclosed. Changed 3D results retain signed Δvolume, Δtriangles, and bounding-box minimum/maximum/size deltas; 2D and missing metrics are labeled honestly. The fixed-height status bar shows a compact summary and offers the complete readout through a keyboard-accessible disclosure. | Q-0035 governs only owner-selected alternative metric semantics. The conservative implementation and focused acceptance evidence are complete; exact hosted evidence is required before the slice merges. |
| FR-15.1 render cache | The application keeps successful 2D/3D results in a typed, content-addressed 512 MiB memory LRU. Desktop disk caching is default-off and selected independently for each opened project; scratch work is ineligible. Records live under the app cache directory, use a 4 MiB IPC-safe record cap and sidecar LRU metadata, and support cold-start hits. Disabling a project stops its disk-tier reads and writes without deleting records; **Clear this project's disk render cache** deletes that project's durable records. Unchanged renders disclose **cached** in the status bar; failures, stale work, and superseded work never enter either tier. | Q-0036 parks only owner-selected alternative cache-failure, key, eviction, or disk-placement semantics. |
| FR-15.13 thumbnails | Strict 240x160 PNG capture, binary persistence, Welcome newest-per-project presentation, file-tree hover/focus previews, fixed-size restoration, and stale-capture race handling are implemented with focused lifecycle evidence. | Exact hosted walkthrough evidence remains; Q-0037 parks only the M4/M5 timeline boundary and alternate recent-project selection policy. |
| C10 AI assist | Truthful setup/readiness rail state, direct OpenAI-compatible/Anthropic/local HTTP transport, cancellable streaming, fixed OpenSCAD system prompt, configured-model label, fenced-code extraction, markdown, copy and insert-at-cursor actions, per-hunk diff review, active-document `ai-panel` edit dispatch, live source/diagnostic/parameter context toggles including the latest explicit viewer PNG, bounded document-scoped conversation persistence with hydration and clear action, and a default-10 capped agent-loop runner are implemented with focused and aggregate evidence. | Provider-secret redaction review, UI agent-mode/tool-handler wiring, auto-apply policy, and hosted AC-10.c/d evidence remain. |
| C11 command/history and MCP | Appendix B tool metadata and strict request validation, newline-bounded stdio framing, an explicit off/running lifecycle controller, a session-local Settings toggle defaulting off, a Tauri stdin/stdout bridge, JSON-RPC dispatch, and a bounded pending-review queue are implemented. Settings provides independent `allow once` / `allow for this session` / `deny` gates for file writes and parameter changes; a denial returns structured JSON-RPC error `-32001`, and an allow-once grant resets after the accepted request. The History rail shows each queued external mutation; file proposals receive an in-app diff and parameter batches show their exact values. Only explicit approval dispatches the change through the command bus with an `external-agent` history badge; denial leaves project state unchanged. The screenshot tool captures the current rendered 3D canvas at its requested pixel size and returns a base64 PNG. | A real desktop subprocess walkthrough and hosted AC-11.b/c evidence remain. |

## Requirements

- Node.js 24 or newer
- pnpm 11.7.0
- Rust 1.96.0 (pinned by `rust-toolchain.toml`) for the desktop shell
- OpenSCAD development snapshot 2026.06.12 for the A-7 engine pin

## Quick start

1. Clone the repository.
2. Run `pnpm install --frozen-lockfile`.
3. Run `pnpm test`.
4. Run `pnpm dev` for the browser-hosted UI shell.
5. Run `pnpm desktop dev` for the native engine path.

The editor and local project features run from a public clone. Browser rendering additionally requires the separately distributed OpenSCAD JavaScript/WASM pair. Those bytes are locally staged for validation but are ignored and not publicly deployed while the owner resolves the Q-0033 distribution-materials decision; without them, the browser keeps editing available and reports the engine load failure honestly.

When an approved pair is present, the browser-hosted shell initializes it in a dedicated module worker and caches the verified artifacts atomically in IndexedDB after the first successful load. Web builds copy the versioned engine files as separately fetched static assets. Tauri uses the dedicated `build:desktop` mode, which excludes that public directory so the GPL engine artifact is not embedded in desktop resources or installers.

## Basic use

Open the desktop shell, enter a model such as `cube([10, 20, 30]);` in the active `.scad` document, and choose **Render preview** or press F5. Press F6 for a full render. ScadMill renders real geometry through the pinned native OpenSCAD engine, shows the measured model in the orbitable viewer, and streams structured diagnostics alongside the captured engine log.

## Engine configuration

The desktop path checks a bundled engine candidate first, then the user-configured executable path, `SCADMILL_OPENSCAD`, and finally `PATH`. Only the recorded OpenSCAD 2026.06.12 snapshot is accepted. If discovery fails or finds another version, use the in-app **Configure engine** fix-it to save the full pinned executable path and retry. Direct process control is retained for reliable cancellation and timeout cleanup.

## Verification

`pnpm check:generated`, `pnpm check:wasm-workflow`, `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm test:e2e`, `pnpm build`, `pnpm check:provenance`, `pnpm check:licenses all`, `cargo fmt -- --check`, `cargo clippy -- -D warnings`, and the Rust test suite form the local quality gate. Run the retained M2 browser evidence lane with `pnpm.cmd exec playwright test --config tests/e2e/m2-gate.playwright.config.ts`; set `SCADMILL_GATE_ARTIFACT_DIR` to retain its attestations, screenshot, HTML, traces, JSON report, exact runner copies, and SHA-256 hashes outside the repository. Run `pnpm profile:viewer` for the hardware-disclosing two-million-triangle orbit profile. Fully stage the candidate before applying the owner-baseline qualification so the evidence envelope can bind the exact staged tree and profiler harness. On the owner-designated Radeon 780M benchmark host, set `SCADMILL_PERF_HARDWARE_QUALIFICATION=owner-baseline-amd-radeon-780m`; the harness rejects that qualification unless the unmasked renderer identifies AMD Radeon 780M and the source identity remains unchanged throughout sampling. This is an evidence baseline, not a minimum supported-hardware claim. The packaged Windows lane is `scripts/windows/run-packaged-desktop-evidence.ps1`; invoke it through `powershell.exe -NoProfile -ExecutionPolicy Bypass -File` from a clean committed worktree, with a fresh output directory outside that worktree plus explicit release-engine, `tauri-driver`, `vcruntime140.dll`, `vcruntime140_1.dll`, matching EdgeDriver, and fixed WebView2 paths. It accepts no application path: the wrapper installs frozen dependencies, cleans the canonical desktop target, runs the pinned Tauri release builder with locked Cargo inputs so `frontendDist` is embedded, rejects any source-tree change, and stages only that just-built executable. Its fixed runtime and remote-debugging switch exist only inside the disposable evidence launch and are not product bundling. After changing `src/ui/editor/openscad.grammar`, run `pnpm generate:openscad-parser` and commit both generated TypeScript files. Install the acceptance-test browser once with `pnpm exec playwright install chromium`. Run the Rust commands with `--manifest-path` for both manifests under `src/native-engine` and `src/desktop-shell/src-tauri`. Both npm and Rust dependency-license lanes are green after the owner-approved Unicode-3.0 policy addition. The owner-supplied independence gate runs only in isolated GitHub Actions and must never be executed locally.

Build the Windows installer from `src/desktop-shell` with `pnpm exec tauri build --bundles nsis --ci -- --locked`. The setup embeds the offline WebView2 installer and installs for the current user. Windows MSVC builds use Rust's supported static CRT mode, so the installed `scadmill.exe` must not import `VCRUNTIME140.dll` or `VCRUNTIME140_1.dll`; the hosted installer lane checks the PE import table before signing. Signing uses the repository's service-principal secrets directly with Azure Artifact Signing—never `azure/login`—then fails closed on `Get-AuthenticodeSignature` before hashing or upload.

## Provenance and privacy

Every coherent change carries a machine-readable entry under `provenance/entries/`. See [`PROVENANCE.md`](PROVENANCE.md) and [`PRIVACY.md`](PRIVACY.md).

## Contributing

See [`CONTRIBUTING.md`](CONTRIBUTING.md) for the clean-room rules, setup, and verification expectations.

## License

No public reuse license has been selected yet. Until the owner makes the M3 licensing decision, all rights are reserved; see [`LICENSE`](LICENSE).
